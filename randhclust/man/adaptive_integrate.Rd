% Generated by roxygen2: do not edit by hand
% Please edit documentation in _main.Rmd.
\name{adaptive_integrate}
\alias{adaptive_integrate}
\title{Adaptive grid integration for pdf evaluation}
\usage{
adaptive_integrate(
  f,
  observed_target,
  wide_grid_min,
  wide_grid_max,
  num_coarse,
  num_fine,
  qlow = 0.005,
  qhigh = 0.995,
  buffer = 3
)
}
\arguments{
\item{f}{A function that takes a single numeric input and returns the
(possibly unnormalized) probability density at that point.}

\item{observed_target}{Numeric. The observed value of the test statistic
for which to compute a p-value.}

\item{wide_grid_min}{Numeric. The minimum value of the initial wide grid.}

\item{wide_grid_max}{Numeric. The maximum value of the initial wide grid.}

\item{num_coarse}{Integer. The number of points to use in the coarse grids
for both passes.}

\item{num_fine}{Integer. The number of points to use in the fine grid for
the first pass. The second pass uses \code{num_fine * 2} points.}

\item{qlow}{Numeric. The lower quantile for defining the narrower interval
(default: 0.005).}

\item{qhigh}{Numeric. The upper quantile for defining the narrower interval
(default: 0.995).}

\item{buffer}{Numeric. Factor by which to expand the narrower interval width
on each side. The final grid spans from \code{q_low - buffer * width} to
\code{q_high + buffer * width}.}
}
\description{
Performs two-pass adaptive grid refinement to efficiently evaluate a
probability density function. First identifies the main part of support on a
wide grid, then refines evaluation on a narrower grid focused on the
high-density region.
}